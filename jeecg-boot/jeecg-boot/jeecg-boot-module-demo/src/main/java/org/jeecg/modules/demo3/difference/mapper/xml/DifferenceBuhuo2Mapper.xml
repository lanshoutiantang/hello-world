<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.demo3.difference.mapper.DifferenceBuhuo2Mapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE
		FROM  differencebuhuo2
		WHERE
			 shopid = #{mainId} 	</delete>

	<select id="selectByMainId" parameterType="java.lang.String" resultType="org.jeecg.modules.demo3.difference.entity.DifferenceBuhuo2">
		SELECT *
		FROM  differencebuhuo2
		WHERE
			 shopid = #{mainId} 	</select>

	<select id="getShopid" resultType="java.lang.String">
		select shopid from differencebuhuo where id = #{id}
	</select>
</mapper>
