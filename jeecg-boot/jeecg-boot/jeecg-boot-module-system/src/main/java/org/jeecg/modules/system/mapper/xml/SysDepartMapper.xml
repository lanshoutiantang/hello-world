<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.SysDepartMapper">

    <select id="getOrgLength" parameterType="String" resultType="java.lang.String">
        select org_type from sys_depart where id = #{parentId}
    </select>

	<select id="queryUserDeparts" parameterType="String" resultType="org.jeecg.modules.system.entity.SysDepart">
	   select * from sys_depart where id IN ( select dep_id from sys_user_depart where user_id = #{userId} )
	</select>

    <!-- 根据username查询所拥有的部门 -->
    <select id="queryDepartsByUsername" parameterType="String" resultType="org.jeecg.modules.system.entity.SysDepart">
        SELECT *
        FROM sys_depart
        WHERE id IN (
            SELECT dep_id
            FROM sys_user_depart
            WHERE user_id = (
                SELECT id
                FROM sys_user
                WHERE username = #{username}
            )
        )
    </select>

    <!-- 根据部门Id查询,当前和下级所有部门IDS -->
    <select id="getSubDepIdsByDepId" resultType="java.lang.String">
		select id from sys_depart where del_flag = '0' and org_code like concat((select org_code from sys_depart where id=#{departId}),'%')
	</select>

    <!--根据部门编码获取我的部门下所有部门ids -->
    <select id="getSubDepIdsByOrgCodes" resultType="java.lang.String">
		select id from sys_depart where del_flag = '0' and
        <foreach collection="orgCodes" item="item" index="index"  open="(" separator="or" close=")">
            org_code LIKE CONCAT(#{item},'%')
        </foreach>
	</select>


    <select id="getParentOrgCode" parameterType="java.lang.String" resultType="java.lang.String">
    select org_code from sys_depart where id = #{pId}
</select>

    <select id="getDepartName" parameterType="java.lang.String" resultType="java.lang.String">
    select depart_name from sys_depart where id = #{pId}
</select>

    <select id="getOrgCode" parameterType="java.lang.String" resultType="java.lang.String">
    select dict_code from sys_dict where dict_name like CONCAT(#{chuName},'%')
</select>

    <select id="getParentId" parameterType="java.lang.String" resultType="java.lang.String">
    select id from sys_category where name = #{chuName}
</select>

    <select id="getDepartNameById" parameterType="java.lang.String" resultType="java.lang.String">
    select name from sys_category where code = #{departID}
</select>

    <select id="getDepartNameCsCodeById" parameterType="java.lang.String" resultType="java.lang.String">
    select cg_cs_code from sys_category where code = #{departID}
</select>

    <select id="getIdList" parameterType="java.util.List" resultType="java.lang.String">
        select * from sys_depart
        <where>
            <foreach collection="list"  open="parent_id in ("  close= ")" item="id" separator=",">
                #{id}
            </foreach>
        </where>

    </select>

    <select id="getCaigouxianId" parameterType="java.lang.String" resultType="java.lang.String">
    select id from sys_category where pid = #{s};
</select>

    <select id="getCodeById" parameterType="java.lang.String" resultType="java.lang.String">
    select org_code from sys_depart where id = #{parentID}
</select>

    <select id="getParentIdByCode" parameterType="java.lang.String" resultType="java.lang.String">
    select id  from sys_category where code = (select org_code from sys_depart where id = #{parentID})
</select>
</mapper>